# Core ML
torch>=2.0
torchaudio>=2.0
transformers>=4.35.0

# MERT model (loaded via transformers)
# Model ID: m-a-p/MERT-v1-330M (downloaded automatically from HuggingFace)

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Drum source separation (rhythm triplet generation)
demucs>=4.0.0

# Numerical / scientific
numpy>=1.24.0
scipy>=1.10.0

# Visualization (probe evaluation figures)
matplotlib>=3.7.0

# Optional: faster nearest-neighbor search in probe evaluation
# faiss-cpu>=1.7.4   # or faiss-gpu for GPU support

# JASCO triplet generation:
#   Install separately following https://github.com/facebookresearch/audiocraft
#   Then set: export JASCO_ROOT=/path/to/jasco-audiocraft
